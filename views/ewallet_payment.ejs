<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>E-Wallet Payment</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="/css/app.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <%- include('partials/navbar', { active: 'orders' }) %>

  <main class="container my-4">
    <% if (error && error.length) { %>
      <div class="alert alert-danger"><%= error[0] %></div>
    <% } %>
    <% if (success && success.length) { %>
      <div class="alert alert-success"><%= success[0] %></div>
    <% } %>

    <div class="card shadow-sm">
      <div class="card-body">
        <div class="d-flex justify-content-between align-items-start mb-3">
          <div>
            <h4 class="mb-1">Pay with E-Wallet</h4>
            <div class="text-muted">Order <span class="fw-semibold">#<%= order.referenceId %></span></div>
          </div>
          <div class="text-end">
            <div class="text-muted" style="font-size:.9rem;">Wallet Balance</div>
            <div class="fw-bold">$<%= Number(wallet?.balance || 0).toFixed(2) %></div>
          </div>
        </div>

        <div class="row">
          <div class="col-lg-8">
            <div class="table-responsive mb-3">
              <table class="table table-striped align-middle">
                <thead class="table-light">
                  <tr>
                    <th>Product</th>
                    <th class="text-end" style="width:120px;">Unit</th>
                    <th class="text-end" style="width:80px;">Qty</th>
                    <th class="text-end" style="width:140px;">Subtotal</th>
                  </tr>
                </thead>
                <tbody>
                  <% if (order.items && order.items.length) { %>
                    <% order.items.forEach(it => { %>
                      <tr>
                        <td>
                          <div class="fw-semibold"><%= it.product_name %></div>
                          <div class="text-muted" style="font-size:0.85rem;">ID: <%= it.productID %></div>
                        </td>
                        <td class="text-end">$<%= parseFloat(it.unit_price).toFixed(2) %></td>
                        <td class="text-end"><%= it.quantity %></td>
                        <td class="text-end fw-semibold">$<%= parseFloat(it.subtotal).toFixed(2) %></td>
                      </tr>
                    <% }) %>
                  <% } else { %>
                    <tr><td colspan="4" class="text-center text-muted">No items found</td></tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>

          <div class="col-lg-4">
            <div class="border rounded p-3 bg-light">
              <div class="mb-2 fw-semibold">Payment Summary</div>
              <div class="d-flex justify-content-between mb-2"><div class="text-muted">Total</div><div class="fw-bold text-danger">$<%= Number(order.total || 0).toFixed(2) %></div></div>
              <div class="d-flex justify-content-between"><div class="text-muted">Wallet Balance</div><div>$<%= Number(wallet?.balance || 0).toFixed(2) %></div></div>
              <hr>
              <div class="text-muted" style="font-size:.9rem;">
                If your balance is insufficient, you’ll be returned to cart with an error.
              </div>
            </div>

            <form class="mt-3" method="POST" action="/payments/ewallet/pay">
              <input type="hidden" name="orderId" value="<%= order.id %>">
              <button type="submit" class="btn btn-success w-100">Pay Now</button>
            </form>

            <a class="btn btn-outline-secondary w-100 mt-2" href="/orders/confirm/<%= order.id %>">Back</a>
          </div>
        </div>

      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
