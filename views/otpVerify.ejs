<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Two-Factor Authentication - Supermarket App</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body class="bg-page">
  <%- include('partials/navbar', { active: 'login', user }) %>

  <main class="app-main">
    <div class="container">
      <div class="card shadow-sm form-card">
        <div class="card-body">
          <h3 class="mb-3 text-center">
            <% if (showQr) { %>
              Set up Two-Factor Authentication
            <% } else { %>
              Two-Factor Verification
            <% } %>
          </h3>

          <% if (showQr) { %>
            <p class="text-muted text-center mb-4">
              Scan the QR code below with Google Authenticator (or a similar app), then enter the 6-digit code.
            </p>

            <div class="d-flex flex-column align-items-center mb-4">
              <img src="<%= qrData %>" alt="OTP QR Code" style="width:220px;height:220px;" class="mb-3" />
              <% if (secretBase32) { %>
                <p class="text-muted mb-0">
                  Can’t scan the QR? Add this key manually in your authenticator app:
                  <br>
                  <strong><%= secretBase32 %></strong>
                </p>
              <% } %>
            </div>
          <% } else { %>
            <p class="text-muted text-center mb-4">
              Enter the 6-digit code from your authenticator app to complete login.
            </p>
          <% } %>

          <% if (error && error.length) { %>
            <div class="alert alert-danger text-center"><%= error[0] %></div>
          <% } %>

          <form method="POST" action="/otp/verify" class="row g-3 justify-content-center">
            <div class="col-md-6">
              <label class="form-label">6-digit authentication code</label>
              <input
                type="text"
                name="token"
                maxlength="6"
                class="form-control text-center"
                inputmode="numeric"
                pattern="[0-9]*"
                required
              >
            </div>

            <div class="col-12">
              <button class="btn btn-primary w-100">Verify</button>
            </div>

            <div class="col-12 text-center">
              <a href="/login" class="text-muted">Back to login</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
